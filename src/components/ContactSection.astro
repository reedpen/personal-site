---
import type { SiteContent } from "../data/site";

type Props = SiteContent["contact"];

const { heading, body, email, links } = Astro.props;
---

<section class="contact" id="contact" aria-labelledby="contact-title">
  <p class="prompt">available for a conversation</p>
  <h2 id="contact-title">{heading}</h2>
  <p>{body}</p>
  <address>
    <a class="contact-email" href={`mailto:${email}`}
      >{email} <span aria-hidden="true">→</span></a
    >
  </address>
  <ul class="inline-links" aria-label="Other contact links">
    {
      links.map((link) => (
        <li>
          <a href={link.href}>[{link.label}]</a>
        </li>
      ))
    }
  </ul>
</section>
